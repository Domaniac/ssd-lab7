name: ESLint Code Analysis
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  eslint:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Code
      uses: actions/checkout@v4
    - name: Install Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
    - name: Install Dependencies
      run: npm install eslint @microsoft/eslint-formatter-sarif eslint-plugin-security --save-dev
    - name: Create Reports Directory
      run: mkdir -p reports
    - name: Run ESLint
      run: npx eslint --config .eslintrc.cjs --format=@microsoft/eslint-formatter-sarif --output-file=reports/eslint-results.sarif . # Use .eslintrc.cjs
    - name: Upload SARIF report
      uses: github/codeql-action/upload-sarif@v3
      with:
        sarif_file: reports/eslint-results.sarif